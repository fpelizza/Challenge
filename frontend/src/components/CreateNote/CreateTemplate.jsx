import Modal from "react-modal";
import CreateNote from './CreateNote'

const CreateTemplate = ({open, handleClose, notes, handleAddNote}) => {
    return (
        <>
        </>
    );
};

export default CreateTemplate;
